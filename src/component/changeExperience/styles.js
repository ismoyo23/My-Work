import {StyleSheet} from 'react-native';

let styles = StyleSheet.create({
  badge: {
    zIndex: 1,
    position: 'absolute',
    backgroundColor: 'red',
    width: 90,
    height: 30,
    borderRadius: 30,
    marginTop: 250,
  },
});

export default styles;
